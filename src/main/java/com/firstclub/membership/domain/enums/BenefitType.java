package com.firstclub.membership.domain.enums;

public enum BenefitType {
    PERCENTAGE_DISCOUNT,   // applies X% off cart total or category
    FREE_DELIVERY,         // waives delivery fee
    PRIORITY_SUPPORT,      // sets a flag for support routing
    EXCLUSIVE_COUPON       // attaches a coupon code to order
}
